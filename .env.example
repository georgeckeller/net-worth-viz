# =============================================================================
# Net Worth Viz — Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# IMPORTANT: Never commit .env.local — it is gitignored by default.
# =============================================================================

# Google Sheet ID (from the spreadsheet URL)
# Example: https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit
VITE_SHEET_ID=

# SHA-256 hash of your dashboard password
# Generate with:
#   node -e "console.log(require('crypto').createHash('sha256').update('your-password').digest('hex'))"
VITE_PASSWORD_HASH=

# Data refresh interval in milliseconds (default: 60000 = 60s)
VITE_REFRESH_INTERVAL=60000

# Cloud Functions base URLs (from terraform output or Firebase console)
VITE_API_URL=https://us-central1-YOUR_PROJECT_ID.cloudfunctions.net/getAssets
VITE_VERIFY_PASSWORD_URL=https://us-central1-YOUR_PROJECT_ID.cloudfunctions.net/verifyPassword

# Smoke test password (plaintext — used only by scripts/smoke-test.sh)
SMOKE_TEST_PASSWORD=
